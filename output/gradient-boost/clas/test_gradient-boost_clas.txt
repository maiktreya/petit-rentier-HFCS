gbm(formula = riquezanet ~ sex + bage + renthog + class, distribution = "bernoulli", 
    data = train_set, weights = facine3, n.trees = 500, interaction.depth = 4, 
    shrinkage = 0.01)
A gradient boosted model with bernoulli loss function.
500 iterations were performed.
There were 4 predictors of which 0 had non-zero influence.
< table of extent 0 x 2 >
httpgd 
     2 
httpgd 
     2 
httpgd 
     2 
cy (Classification): NaN"
